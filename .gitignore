# Python artifacts
__pycache__/
*.pyc
.ipynb_checkpoints/

# Model files — distribute via Zenodo
models/
models_sfvc2026/

# weights/ holds all models; only the 4 named checkpoints+scaler are git-tracked
weights/folds/
weights/MutPred-PPI_sahni.pt
weights/v1_0/

# Results (generated by running scripts — not source)
/results/
/results_revisions/

# Data caches and large intermediates
data_caches/
datasets_unpublished/
logs/

# Raw and derived VarChAMP data (unpublished, local only)
data/

# Training + inference datasets (VarChAMP unpublished; SF/Sahni CSVs documented as Zenodo downloads)
datasets/

# Internal documents (LaTeX source, analysis tracking, ad hoc run scripts)
main.tex
supplement.tex
ANALYSIS_STATUS.md
REPRODUCIBILITY.md
# Root-anchored: these are the ad hoc drivers at the repo root.  Unanchored,
# this also swallowed examples/*/run_example.sh -- the exact file both quickstart
# READMEs tell the reader to run.
/run_*.sh

# Generated figure images (symlinks or PNGs)
figures/*.png
figures/brca1/
figures/brca1_extra/

# Scratch and archived files
*.tar.gz
*.log
ross_MutInt_group_cross_validation_example_results.pkl
.claude/

# Archived obsolete/superseded content (not part of the pipeline)
archive/

# Internal Zenodo packaging checklist (not for public docs/)
ZENODO_CHECKLIST.md

# Reproducibility test scratch (generated)
repro_test/

# Symlinks to large machine-local data (scripts/link_external.sh)
external/
archive/
figures/
brca1/
brca1_extra/
scripts/
*.egg-info/
tests/_scratch/
